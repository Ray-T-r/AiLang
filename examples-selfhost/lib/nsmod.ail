// A helper module imported (aliased) by namespacing.ail. Lives in lib/ (helper
// modules without fixtures belong here — verify.sh FAILS on a fixture-less root sample).
fn rect(w:i64, h:i64) -> i64 { w * h }
fn tri(b:i64, h:i64) -> i64 { (b * h) / 2 }
