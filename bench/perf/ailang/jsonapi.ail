mu count := 0
mu total := 2
lp i in 0..50000 {
  age := 18 + (i % 52)
  if age >= 40 {
    rec := "{\"id\":${i},\"name\":\"user_${i}\",\"age\":${age}}"
    if count > 0 { total += 1 }
    total += len(rec)
    count += 1
  }
}
println(count)
println(total)
